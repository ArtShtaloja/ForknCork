PORT=3000
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=fork_n_cork
SESSION_SECRET=anothersecret
NODE_ENV=development

# Comma-separated list of allowed CORS origins (your frontend URL on Render)
# Example: https://forkncork.onrender.com,https://www.forkncork.com
CORS_ORIGIN=http://localhost:3000

# For Aiven / remote MySQL, you can use a single connection URL instead of
# individual DB_* vars. If set, DATABASE_URL takes priority.
# DATABASE_URL=mysql://user:pass@host:port/database?ssl-mode=REQUIRED

